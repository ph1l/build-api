#Required Software (Debian Squeeze)
$ sudo apt-get install python-flask python-redis

#Installation
$ cp build-api.cfg ~/.build-api
$ sudo mkdir /var/lib/build-api
$ sudo chown phil /var/lib/build-api
